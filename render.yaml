services:
  - type: web
    name: stats-engine-matchstats
    runtime: docker
    dockerfilePath: ./Dockerfile
    plan: free
    envVars:
      - key: MATCHSTATS_URL
        value: https://matchstats.us.ffesports.com/
